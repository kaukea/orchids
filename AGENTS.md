# orchids — project rules

Data package: agents, skills, rule files. No distribution code — that's
`serialseb/kauk`. See `README.md`.

- Edits here take effect in every consuming repo on its next session.
