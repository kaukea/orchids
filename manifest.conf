# orchids manifest — <skill> <role>
# Roles are fixed here (dev · infra · org · all). Per-repo delivery is tuned in the
# repo's .ai.toml: [role] sections with skillname = "exclude"|"copy"|"link"|"local",
# ["*"] for defaults. Missing/empty .ai.toml = everything, linked.
read-agents all
agent-behaviour all
git-commit all
skill-sync all
handover all
doing-skills org
orchestrator org
groom org
history-rewrite org
workflow dev
workflow-complete dev
clean-code dev
readme-sync dev
diagnostics dev
coding-dotnet dev
coding-tofu dev
coding-lmstudio dev
software-catalog infra
chain-of-custody infra
digital-signature infra
forensic-acquisition infra
icloud infra
machine-access infra
read-apfs infra
reverse-engineering-files infra
write-to-s3 infra
